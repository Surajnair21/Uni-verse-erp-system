{
  "name": "universe-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "landing",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n FRONTEND,LANDING,BACKEND -c auto \"npm run dev -w frontend\" \"npm run dev -w landing\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w landing && npm run build -w backend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "pptxgenjs": "^4.0.1"
  }
}
